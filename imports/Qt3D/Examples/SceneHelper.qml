import QtQml 2.15

QtObject {
    function findEntity(loader, name) {
        return null
    }

    function addListEntry(list, entry) {
    }

    function removeFromScene(loader, names) {
    }

    function addBasicMaterials(loader, material, names) {
    }

    function addTextureMaterial(loader, material, name) {
    }

    function replaceMaterial(loader, name, material) {
    }

    function searchCamera(loader, name) {
    }
}